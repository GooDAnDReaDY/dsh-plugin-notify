# Project instructions

- Project: `dsh-plugin-notify`
- DEV: `/mnt/external/Project/DEV/dhsplugins/dsh-plugin-notify`
- Package route: private `@goodandready-private/dsh-plugin-notify` in GitHub Packages.
- OPT: not applicable; this is a published DSH package installed by profile.
- Status: active, verified 27.08.2026 by package tests and DSH test-profile smoke.
- Runtime identity must match in `package.json`, `cordis.patch.yml`, and the host bundle export.
- Do not place credentials, webhook values, machine paths, or local install paths in tracked files.
- Development and release work is performed only in Git worktrees with `git-codex`.
- The permanent `@goodandready/dsh-lanmode` test plugin and Hermes files are out of scope.

See `index.md` and `docs/` for the verified project map and procedures.
